# ComfyUI 远程调用工具 - 发布指南

本文档说明如何**手动上传到 GitHub**，以及如何**同步到 ComfyUI 社区**（ComfyUI Manager + Comfy Registry）。

---

## 一、手动上传到 GitHub

### 1. 配置 Git（首次使用需执行）

打开 PowerShell 或 CMD，执行：

```bash
git config --global user.email "你的邮箱@example.com"
git config --global user.name "你的GitHub用户名"
```

### 2. 在 GitHub 创建仓库

1. 打开 https://github.com/new
2. **Repository name**：填 `ComfyUI-RemoteCall`（或 `ComfyUI-远程调用工具`）
3. **Description**：填 `ComfyUI 远程调用工具 - 远程执行工作流并拉取产物`
4. 选择 **Public**
5. **不要**勾选 "Add a README file"、"Add .gitignore"、"Choose a license"
6. 点击 **Create repository**

### 3. 推送代码到 GitHub

在 PowerShell 中执行（注意替换 `YOUR_USERNAME` 为你的 GitHub 用户名）：

```powershell
cd "c:\Users\XIU\Desktop\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-批量测试工具"

# 若尚未初始化（本仓库已初始化可跳过）
# git init

git add .
git commit -m "Initial commit: ComfyUI 远程调用工具"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ComfyUI-RemoteCall.git
git push -u origin main
```

**推送时提示输入密码**：GitHub 已不支持账号密码，需使用 **Personal Access Token**：
- 用户名：你的 GitHub 用户名
- 密码：粘贴你的 [Personal Access Token](https://github.com/settings/tokens)（创建时勾选 `repo` 权限）

---

## 二、同步到 ComfyUI Manager（旧版社区列表）

ComfyUI Manager 通过 `custom-node-list.json` 维护节点列表，需要向 **ComfyUI-Manager** 仓库提交 Pull Request。

### 1. Fork 并编辑

1. 打开 https://github.com/ltdrdata/ComfyUI-Manager
2. 点击右上角 **Fork**，将仓库 Fork 到你的账号
3. 在你的 Fork 中打开 `custom-node-list.json`
4. 点击 **Edit**（铅笔图标）编辑

### 2. 添加节点条目

在 `custom_nodes` 数组末尾（最后一个 `}` 之前）添加一条，注意逗号：

```json
,
{
  "author": "你的GitHub用户名",
  "title": "ComfyUI 远程调用工具",
  "id": "comfyui-remote-call",
  "reference": "https://github.com/YOUR_USERNAME/ComfyUI-RemoteCall",
  "files": [
    "https://github.com/YOUR_USERNAME/ComfyUI-RemoteCall"
  ],
  "install_type": "git-clone",
  "description": "远程调用另一台 ComfyUI 实例执行工作流，支持上传资源、参数覆盖、轮询结果并拉取产物（图片、视频、音频）。"
}
```

将 `YOUR_USERNAME` 替换为你的 GitHub 用户名。

### 3. 提交 Pull Request

1. 点击 **Commit changes**，填写提交信息如：`Add ComfyUI 远程调用工具`
2. 打开 https://github.com/ltdrdata/ComfyUI-Manager/compare
3. **base repository** 选 `ltdrdata/ComfyUI-Manager`，**base** 选 `main`
4. **head repository** 选你的 Fork，**compare** 选你刚提交的分支
5. 点击 **Create pull request**，填写标题和说明
6. 等待维护者合并（通常数天）

合并后，用户即可在 ComfyUI Manager 中搜索「远程调用」或「RemoteCall」安装。

---

## 三、同步到 Comfy Registry（新版官方注册表）

Comfy Registry 是官方新注册表，新版 ComfyUI Manager 优先从这里安装。需要先注册发布者并配置元数据。

### 1. 注册发布者

1. 打开 https://registry.comfy.org/
2. 使用 GitHub 登录
3. 创建 **Publisher**（发布者），获取唯一的 **Publisher ID**（如 `your-publisher-id`）

### 2. 创建 API 密钥

1. 打开 https://registry.comfy.org/nodes
2. 选择你的发布者，创建 **Registry Publish API Key**
3. 妥善保存密钥（只显示一次）

### 3. 安装 Comfy CLI

```bash
pip install comfy-cli
```

### 4. 在节点目录初始化元数据

```powershell
cd "c:\Users\XIU\Desktop\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-批量测试工具"
comfy node init
```

按提示填写：
- **Node name**：`comfyui-remote-call`（创建后不可改）
- **Publisher ID**：你的发布者 ID
- **Display name**：`ComfyUI 远程调用工具`
- **Version**：`1.0.0`
- **Repository**：`https://github.com/YOUR_USERNAME/ComfyUI-RemoteCall`
- **Icon**：可选，图标 URL（建议 800x400px 以内）

会生成 `pyproject.toml` 文件。

### 5. 发布到 Registry

**方式 A：手动发布**

```bash
comfy node publish
```

按提示输入 API 密钥即可。

**方式 B：GitHub Actions 自动发布**

1. 在 GitHub 仓库 **Settings → Secrets and variables → Actions** 中新建 Secret：
   - Name：`REGISTRY_ACCESS_TOKEN`
   - Value：你的 API 密钥

2. 在仓库根目录创建 `.github/workflows/publish_action.yml`：

```yaml
name: Publish to Comfy registry
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "pyproject.toml"

jobs:
  publish-node:
    name: Publish Custom Node to registry
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Publish Custom Node
        uses: Comfy-Org/publish-node-action@main
        with:
          personal_access_token: ${{ secrets.REGISTRY_ACCESS_TOKEN }}
```

3. 之后每次修改 `pyproject.toml` 中的版本号并推送到 main，会自动发布。

---

## 四、总结

| 步骤 | 平台 | 说明 |
|------|------|------|
| 1 | GitHub | 手动 push 代码到你的仓库 |
| 2 | ComfyUI Manager | 向 ltdrdata/ComfyUI-Manager 提 PR 编辑 custom-node-list.json |
| 3 | Comfy Registry | 注册发布者 → comfy node init → comfy node publish |

完成以上步骤后，用户可通过：
- **ComfyUI Manager** 搜索安装（需等待 PR 合并）
- **Comfy Registry** 安装（需你先完成发布）

---

## 五、后续更新

- **GitHub**：修改代码后 `git add .` → `git commit -m "更新说明"` → `git push`
- **Comfy Registry**：修改 `pyproject.toml` 中的 `version`，推送后若配置了 Actions 会自动发布
- **ComfyUI Manager**：PR 合并后，用户通过 Manager 的 Update 即可获取最新版，无需再次提 PR
