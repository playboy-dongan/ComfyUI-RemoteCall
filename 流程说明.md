# ComfyUI 远程调用工具 - 流程说明

## 一、整体逻辑

```
┌─────────────────────────────────────────────────────────────────────────┐
│  远程调用工作流（需在 ComfyUI-A 或独立脚本中运行）                          │
│                                                                         │
│  [工作流上传] → [工作流解析] ──工作流_JSON──┐                             │
│                                            │                             │
│  [传输A] 节点ID=3, 任意=图片1              │                             │
│      ↓ 串联输出                            │                             │
│  [传输B] 节点ID=7, 任意=图片2, 串联输入←A  │                             │
│      ↓ 串联输出                            │                             │
│  [参数覆盖] 16.width=512; 16.height=512   │                             │
│      ↓ 串联输出                            ↓                             │
│  ───────────────────────────────→ [上传提交] ──prompt_id──→ [查询执行结果] │
│      串联输入                    地址、工作流  地址、prompt_id   产物       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ POST /prompt
                                    │ GET /history/{prompt_id}
                                    ↓
┌─────────────────────────────────────────────────────────────────────────┐
│  目标 ComfyUI（ComfyUI-B，被测试的实例）                                  │
│                                                                         │
│  接收提交的任务 → 执行工作流 → 结果写入 history                            │
└─────────────────────────────────────────────────────────────────────────┘
```

## 二、各节点职责

| 节点 | 输入 | 输出 | 作用 |
|------|------|------|------|
| 工作流上传 | 工作流 JSON | 工作流_JSON | 校验并透传 |
| 工作流解析 | 工作流_JSON | 可配置输入、输出节点、工作流_JSON | 解析 API 格式 |
| 传输节点 | 节点ID、任意、串联输入 | 串联输出 | 收集 {节点ID: 文件数据} |
| 参数覆盖 | 参数覆盖、串联输入 | 串联输出 | 解析 节点ID.字段=值，合并到串联数据 |
| 上传提交 | 地址、工作流_JSON、串联输入（均为输入端） | prompt_id、是否成功、消息 | 上传资源 → 注入工作流 → POST /prompt |
| 查询执行结果 | 地址、prompt_id（均为输入端） | 产物_图像、产物_视频、产物_音频、是否成功、消息 | 轮询 /history/{prompt_id} → 拉取产物 |

## 三、查询执行结果为何可能拿不到数据？

### 核心问题：同实例死锁

**若远程调用工作流和目标 ComfyUI 是同一个实例**（例如都在 `127.0.0.1:8188`）：

1. 你点击 Queue Prompt，ComfyUI 开始执行「远程调用工作流」
2. 上传提交 执行 → `POST /prompt` 把任务加入**同一实例**的队列
3. 查询执行结果 开始轮询 `GET /history/{prompt_id}`
4. **问题**：ComfyUI 队列是串行的，当前正在跑的是「远程调用工作流」
5. 你刚提交的任务排在后面，要等当前工作流跑完才会执行
6. 查询执行结果 在等任务完成，但任务要等查询执行结果所在的工作流先结束
7. **形成死锁**：任务永远不会执行，history 里永远没有该 prompt_id

### 正确用法：双实例

- **ComfyUI-A**：运行远程调用工作流（上传提交、查询执行结果等）
- **ComfyUI-B**：被测试的目标实例，地址填 B（如 `http://127.0.0.1:8189`）

这样：
- 上传提交 向 B 提交任务
- B 独立执行，不受 A 阻塞
- 查询执行结果 向 B 轮询，能拿到结果

### 单实例时的变通

若只有一个 ComfyUI 实例，可以：

1. **分两次跑**：
   - 第一次：只跑 上传提交，拿到 prompt_id
   - 等任务在 History 里出现后，第二次：只跑 查询执行结果，手动填入 prompt_id
2. **用「仅查询一次」**：先提交，等一段时间后，再单独跑查询节点，手动填 prompt_id

## 四、数据流检查清单

- [ ] 上传提交 和 查询执行结果 的「地址」是否一致？
- [ ] 远程调用工作流是否在**另一个** ComfyUI 实例中运行？
- [ ] 目标工作流是否有 SaveImage 等输出节点？
- [ ] 目标工作流是否为 API 格式？
